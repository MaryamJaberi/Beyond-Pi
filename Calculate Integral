import sympy as sp

# Define the variables and limits
x = sp.symbols('x')
L = 10  # Length of the lines

# Define the integral
integral = sp.integrate(L, (x, 0, L))

# Calculate the area
area = integral.evalf()

print(f"Approximated area of quarter circle: {area:.6f} square units")
